<Window x:Class="diplom.views.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Loading..." Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" ResizeMode="NoResize">

    <Border Background="{DynamicResource PrimaryBackground}" CornerRadius="16" 
            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.5" Color="Black"/>
        </Border.Effect>
        
        <Grid>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <!-- Logo/Brand -->
                <TextBlock Text="WORKFLOW" FontSize="32" FontWeight="Bold" 
                           Foreground="{DynamicResource AccentColor}" 
                           HorizontalAlignment="Center"/>
                
                <TextBlock Text="TimeFlow" FontSize="14" 
                           Foreground="{DynamicResource SecondaryText}" 
                           HorizontalAlignment="Center" Margin="0,4,0,0"/>
                
                <!-- Loading indicator -->
                <Grid Margin="0,40,0,0" Width="200">
                    <ProgressBar x:Name="LoadingProgress" Height="4" IsIndeterminate="True"
                                 Background="{DynamicResource SurfaceBrush}"
                                 Foreground="{DynamicResource AccentColor}"
                                 BorderThickness="0">
                        <ProgressBar.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="2"/>
                            </Style>
                        </ProgressBar.Resources>
                    </ProgressBar>
                </Grid>
                
                <!-- Status text -->
                <TextBlock x:Name="StatusText" Text="Connecting to database..." 
                           FontSize="12" Foreground="{DynamicResource MutedText}" 
                           HorizontalAlignment="Center" Margin="0,16,0,0"/>
            </StackPanel>
            
            <!-- Version -->
            <TextBlock Text="v1.0.0" FontSize="11" 
                       Foreground="{DynamicResource MutedText}" 
                       HorizontalAlignment="Right" VerticalAlignment="Bottom"
                       Margin="0,0,16,12"/>
        </Grid>
    </Border>
</Window>
