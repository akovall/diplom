<Window x:Class="diplom.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        
        xmlns:vm="clr-namespace:diplom.viewmodels"
        xmlns:views="clr-namespace:diplom.views"
        
        Title="TimeFlow - Management System" Height="700" Width="1100"
        WindowStartupLocation="CenterScreen" Background="{DynamicResource PrimaryBackground}">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <views:DashboardView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:TasksViewModel}">
            <views:TasksView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:ProjectsViewModel}">
            <views:ProjectsView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:TimeTrackerViewModel}">
            <views:TimeTrackerView/>
        </DataTemplate>


    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{DynamicResource SidebarBackground}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0.3,0">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="WORKFLOW" Foreground="{DynamicResource PrimaryText}" FontSize="20" FontWeight="Bold" 
                           HorizontalAlignment="Center" Margin="0,30,0,40"/>

                <StackPanel Grid.Row="1">
                    <RadioButton Content="Dashboard" Style="{StaticResource MenuButtonStyle}" 
                                 IsChecked="True"
                                 Command="{Binding NavigateCommand}" CommandParameter="Dashboard"/>

                    <RadioButton Content="Tasks" Style="{StaticResource MenuButtonStyle}"
                                 Command="{Binding NavigateCommand}" CommandParameter="Tasks"/>

                    <RadioButton Content="Projects" Style="{StaticResource MenuButtonStyle}"
                                 Command="{Binding NavigateCommand}" CommandParameter="Projects"/>

                    <RadioButton Content="Time Tracker" Style="{StaticResource MenuButtonStyle}"
                                 Command="{Binding NavigateCommand}" CommandParameter="TimeTracker"/>

                    <RadioButton Content="Settings" Style="{StaticResource MenuButtonStyle}" Margin="0,20,0,0"
                                 Command="{Binding NavigateCommand}" CommandParameter="Settings"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="15,0,15,30">
                    <Button Command="{Binding ToggleThemeCommand}" 
                            Height="40"
                            Background="Transparent"
                            BorderBrush="{DynamicResource ButtonBorder}" BorderThickness="1"
                            Foreground="{DynamicResource SecondaryText}"
                            Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                        BorderBrush="{TemplateBinding BorderBrush}" 
                                        BorderThickness="{TemplateBinding BorderThickness}" 
                                        CornerRadius="8">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding ThemeIcon}" FontSize="14" Margin="0,0,8,0"/>
                                        <TextBlock Text="Theme" FontSize="13"/>
                                    </StackPanel>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="{DynamicResource AccentColor}"/>
                                        <Setter Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Background="{DynamicResource PrimaryBackground}">
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{DynamicResource HeaderBackground}" >
                <Grid Margin="20,0">
                    <TextBlock Text="{Binding CurrentPageTitle}" VerticalAlignment="Center" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryText}"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="Admin" VerticalAlignment="Center" Margin="0,0,15,0" Foreground="{DynamicResource MutedText}"/>
                        <Ellipse Width="35" Height="35" Fill="{DynamicResource AvatarBrush}"/>
                    </StackPanel>
                </Grid>
            </Border>
            <ContentControl Grid.Row="1" Content="{Binding CurrentView}" Margin="20"/>
        </Grid>
    </Grid>
</Window>